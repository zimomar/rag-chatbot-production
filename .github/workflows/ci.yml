name: CI - Local RAG Pipeline

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  lint-test:
    name: Code Quality & Tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: 'pip'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"

      - name: Lint with ruff
        run: ruff check src tests

      - name: Format check with ruff
        run: ruff format --check src tests

      - name: Type check with mypy
        run: mypy src --ignore-missing-imports

      - name: Run unit tests with pytest
        run: pytest tests/ -v

  security:
    name: Security Scan
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: 'pip'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"
          pip install bandit safety

      - name: Security scan with bandit
        run: bandit -r src -c pyproject.toml || bandit -r src -s B101,B603

      - name: Check dependencies for vulnerabilities
        run: safety check --output text || true

  docker-check:
    name: Docker Build & Health Check
    runs-on: ubuntu-latest
    needs: [lint-test]
    steps:
      - uses: actions/checkout@v4

      - name: Create dummy .env
        run: cp .env.example .env

      - name: Build Docker images
        run: docker compose build

      - name: Start services
        run: docker compose up -d

      - name: Wait for services healthcheck
        run: |
          # On attend max 2 minutes que tout soit Healthy
          for i in {1..24}; do
            STATUS=$(docker compose ps --format json)
            echo "Current status: $STATUS"
            # Si on ne trouve plus 'starting', c'est que tout est stable (Healthy ou Unhealthy)
            if [[ ! "$STATUS" == *"starting"* ]]; then
              # Si on ne trouve aucun 'unhealthy', c'est gagné
              if [[ ! "$STATUS" == *"unhealthy"* ]]; then
                echo "All services are HEALTHY!"
                exit 0
              fi
            fi
            sleep 5
          done
          echo "Timeout: Some services failed to become healthy."
          docker compose ps
          docker compose logs
          exit 1

      - name: Cleanup
        if: always()
        run: docker compose down -v
